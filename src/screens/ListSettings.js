import { StyleSheet, Text, View } from "react-native";
import React from "react";

export default function ListSettings({ navigation }) {
  return (
    <View>
      <Text>ListSettings</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
