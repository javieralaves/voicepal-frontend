import { Modal, View, Text, TouchableOpacity, StyleSheet } from "react-native";

const ShareLog = ({ isVisible, onClose, onShare }) => <Text>Hello!</Text>;

const styles = StyleSheet.create({});

export default ShareLog;
